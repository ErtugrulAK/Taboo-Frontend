/*
import React from "react";

function WordCard1(props) {
  return (
    <div className="card">
      <h2>{props.word && props.word.toUpperCase()}</h2>
      <div>
        {props.taboo &&
          props.taboo.map((item, i) => (
            <p key={i}>{item.toUpperCase()}</p>
          ))}
      </div>
    </div>
  );
}

export default WordCard1;
*/